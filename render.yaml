services:
  - type: web
    name: scanpdf-backend
    runtime: docker
    plan: free
    region: singapore
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    dockerCommand: /bin/sh -c "npm run db:deploy && (node dist/src/workers/conversion.worker.js &) && exec node dist/src/server.js"
    healthCheckPath: /health/ready
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: scanpdf-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: scanpdf-redis
          type: keyvalue
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: FRONTEND_URL
        sync: false
      - key: CORS_ORIGINS
        value: ""
      - key: API_URL
        sync: false
      - key: STORAGE_DRIVER
        value: local
      - key: STORAGE_ROOT
        value: /tmp/scanpdf-storage
      - key: OPENAI_MODEL
        value: gpt-5.2
      - key: AI_PROVIDER
        value: openai
      - key: GEMINI_MODEL
        value: gemini-2.0-flash
      - key: MAIL_FROM
        value: ScanPDF <no-reply@scanpdf.vn>
      - key: CLEANUP_INTERVAL_MINUTES
        value: 1440
      - key: CLEANUP_BATCH_SIZE
        value: 200
      - key: SENTRY_TRACES_SAMPLE_RATE
        value: 0.1

  - type: keyvalue
    name: scanpdf-redis
    plan: free
    region: singapore
    ipAllowList: []
    maxmemoryPolicy: allkeys-lru

databases:
  - name: scanpdf-db
    plan: free
    region: singapore
    databaseName: scanpdf
    user: scanpdf
